# Base URLs for the two backend services. Defaults (localhost:3001/3000)
# already match local dev via DotCard-API's docker-compose, so this file is
# only needed if you're pointing at something else.
VITE_DOTCARD_API_URL=http://localhost:3001
VITE_AUTHFORGE_URL=http://localhost:3000
